module.exports = {
  // From Template (Start)
  entry: "./src/main.ts",
  // From Template (End)

  // From Template (Start): Put your normal webpack config below here
  module: {
    rules: require("./webpack.rules")
  },
  // From Template (End)
};
